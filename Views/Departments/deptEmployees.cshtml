@*@model Department*@
@model List<Amnex_Project_Resource_Mapping_System.Models.Employee>

@{
    ViewData["Title"] = "deptEmployees";
}
<link href="~/css/kendo.css" rel="stylesheet" />

<div class="nav-info  mt-3 mb-4">
    <div class="nav-item">
        <a asp-controller="Departments" asp-action="Departments">Departments</a> >
        <a id="departmentDetailsLink" href="#">Details</a> >
        <a>Employees</a>

    </div>
</div>
<div class="mb-5 mt-3">
    <button id="detailsbtn" class="btn btn-primary">Details</button>
    <button id="deptprojects" class="btn btn-primary ">Projects</button>
    <button id="deptemployees" class="btn btn-primary active">Employees</button>
</div>
<div id="deptemployeesgrid">
</div>
@section Scripts{
    <script>
        var deptid = @ViewData["DepartmentId2"];
        console.log("deptidd" + deptid);
        var dataSource = @Html.Raw(Json.Serialize(Model));
    </script>
    <script src="~/js/deptEmployees.js"></script>
}

